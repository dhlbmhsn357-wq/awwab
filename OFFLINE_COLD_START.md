# مشكلة Offline Cold Start — ملخص كامل: التشخيص، الحلول الضعيفة، والحل الحقيقي

## المشكلة كما وصفها المستخدم

> التطبيق أحيانًا يعمل Offline إذا كان مفتوحًا بالفعل، لكنه عند فتحه
> من الصفر بدون إنترنت قد لا يفتح.

كلمة **"أحيانًا"** هي المفتاح اللي قاد للتشخيص الصح — مشكلة متقطّعة
(مش دايمة) معناها فيه عامل زمني بيتحكم فيها، مش خلل ثابت.

---

## الحلول اللي بدت منطقية لكنها كانت واهية (مش هي السبب)

قبل ما نوصل للسبب الحقيقي، فيه شكوك منطقية كان لازم تتفحص — كل واحدة
فيها اتفحصت وطلعت **مش السبب**، لكن كانت خطوة ضرورية عشان نستبعدها
بثقة بدل ما نخمّن:

### 1. "Capacitor بيحمّل من Vercel الحي مش من الملفات المحلية"
شكّ منطقي جدًا — لو صح، كان هيفسّر المشكلة بالكامل (مفيش نت = مفيش
تحميل للواجهة أصلاً). لما اتفحص `capacitor.config.ts` والملف
المُولّد فعليًا جوه الـAPK (`android/app/src/main/assets/
capacitor.config.json`)، طلع مفيش `server.url` خالص — التطبيق أصلاً
بيحمّل الملفات المحلية المجمّعة زي ما المفروض. **حل وهمي: مكانش
موجود من الأساس.**

### 2. "الـService Worker مش بيخزّن كل حاجة محتاجها الواجهة"
منطقي كمان — لو الـSW ناقص ملفات أساسية، الويب/PWA ممكن يفشل يفتح
أوفلاين. اتفحص `sw.js` ولقيته فعلاً بيخزّن كل الأساسيات (HTML، الأيقونات،
المكتبات المتسلّفة محليًا). **لكن الأهم: الـSW أصلاً غير ذي صلة
بمشكلة التطبيق النيتف على أندرويد** — التطبيق المثبّت بيحمّل ملفاته
من الحزمة المحلية (Bundled Assets) مباشرة، مش عن طريق Network
Request يمر على الـSW خالص. **لو ركّزنا الإصلاح هنا بس، المشكلة كانت
هتفضل موجودة 100% على أندرويد.**

### 3. "navigator.onLine مش دقيق"
ده صحيح فعلاً (اتوثّق ومعالج من Phase 4 قبل كده بـ`@capacitor/network`)،
واستخدمناه كجزء من الإصلاح النهائي (بديل أدق: `isOnline` global). لكن
لو كان هو السبب الوحيد، المشكلة كانت هتكون *دايمة* مش *متقطّعة* — لأن
`navigator.onLine` بيرجع `false` صح غالبًا في وضع الطيران الحقيقي.
**عامل مساعد صحيح، لكن مش السبب الجذري للمشكلة المتقطّعة.**

### 4. "الـTimeout بتاع الطلبات (12 ثانية) مش كافي"
كان موجود بالفعل من إصلاح سابق (منع تعليق لا نهائي على أي طلب شبكة).
مساعد فعلاً في تقليل وقت الانتظار، لكن **حتى لو الـTimeout صفر ثانية،
المشكلة الجذرية (تحت) كانت هتفضل موجودة** — لأنها مش مشكلة "طلب بياخد
وقت طويل"، هي مشكلة "الطلب بيرجّع نتيجة غلط تمامًا".

---

## السبب الجذري الحقيقي (اتأكد بفحص الكود المصدري فعليًا)

المشكلة كانت في سطر واحد بيبدو بريء تمامًا:

```js
const { data } = await sbAuth.auth.getSession();
```

بفحص الكود الفعلي لمكتبة supabase-js (`vendor/supabase.js`)، لقيت إن
`getSession()` **مش مجرد قراءة من التخزين المحلي** زي ما التعليق في
كودنا كان مفترض. الدالة بتتحقق من صلاحية التوكن، ولو قرب ينتهي (أو
انتهى فعلاً — مدة صلاحية التوكن الافتراضية في Supabase حوالي **ساعة
واحدة**)، بتحاول **تجدّده عبر نداء شبكة حقيقي من جوّا نفسها**، من غير
ما يبان ده في التوقيع الظاهري للدالة.

```js
// من داخل supabase-js (مبسّطة):
if (tokenExpiredOrNearExpiry) {
  const { data, error } = await this._callRefreshToken(refreshToken); // نداء شبكة!
  if (error) return { data: { session: null }, error }; // ← هنا المشكلة
  return { data: { session: data }, error: null };
}
```

لو الجهاز أوفلاين وقت المحاولة دي، التجديد بيفشل، والدالة بترجع
**`session: null`** — مش الجلسة القديمة المحفوظة (اللي لسه موجودة
وصالحة في `localStorage`). الكود القديم كان بيفسّر `null` ده على إنه
"مفيش تسجيل دخول" ويطرد المستخدم لصفحة تسجيل الدخول.

### ليه كانت "أحيانًا" بالظبط

| السيناريو | التوكن | النتيجة القديمة |
|---|---|---|
| فتح التطبيق أوفلاين **بعد** تسجيل دخول قريب | لسه صالح، مفيش تجديد يتحاول | يفتح عادي ✅ |
| فتح التطبيق أوفلاين **بعد أكتر من ساعة** من آخر اتصال | منتهي، تجديد بيتحاول ويفشل | `session: null` → صفحة الدخول ❌ |

مفيش أي تناقض أو عشوائية — المشكلة كانت **حتمية 100%** بناءً على
الوقت اللي عدّى منذ آخر اتصال بالإنترنت، بس كانت *تبدو* عشوائية لأن
محدش كان بيربط "قد ما فات وقت من آخر مرة كان أونلاين" بالنتيجة.

---

## الحل القوي (الفعلي)

الفكرة الأساسية: **متسألش مكتبة خارجية سؤال ("هل المستخدم مسجّل
دخول؟") في وقت ممكن تدّيك فيه إجابة غلط لأسباب مالهاش علاقة بالسؤال
نفسه (فشل شبكة، مش فشل تسجيل دخول).**

1. **اقرا الجلسة المحفوظة من `localStorage` مباشرة الأول** —
   `readRawLocalSession()` — بدون أي نداء شبكة خالص. ده بيجاوب على
   سؤال "هل الجهاز ده اتسجّل دخوله قبل كده؟" بشكل مضمون 100%، مش
   معتمد على أي حاجة تقدر تفشل.

2. **فرّق بين نوعين مختلفين تمامًا من "الفشل"**:
   - *فشل شبكة/Timeout* (مفيش رد وصل خالص) → كمّل بالجلسة المحلية
     (`authState = 'offline_session'`).
   - *رفض صريح من السيرفر* (رد فعلي بكود HTTP، زي 400/401 — يعني
     التوكن اتلغى فعلاً أو الحساب اتحذف) → دي فعلاً حالة تسجيل خروج
     حقيقية (`authState = 'unauthenticated'`).

   الفرق ده بسيط في الكود (سطر واحد: `typeof error.status==='number'`)
   لكنه جوهر الحل — من غيره، إما هنفتح ثغرة (نسمح لجلسة ملغاة تعدي)
   أو نرجّع لنفس المشكلة (نرفض جلسة صالحة بسبب الشبكة).

3. **حماية شاملة، مش رقعة محلية**: نفس مشكلة "توكن منتهي + استدعاء
   `getSession()` من غير مهلة" كانت موجودة في `SB.req()` كمان (الدالة
   المستخدمة في **كل** نداء API في التطبيق، مش بس عند الفتح) —
   اتصلحت هناك كمان، مش بس في نقطة الدخول.

4. **شبكة أمان أخيرة**: Startup Error Boundary (مهلة 12 ثانية) —
   لو أي سبب تاني غير متوقّع خلّى الإقلاع يعلّق، يظهر "تعذّر تحميل
   أوّاب" بدل Spinner للأبد. ده مش الحل للمشكلة الأساسية، لكنه ضمان
   إن أي مشكلة *تانية* في المستقبل متبقاش صامتة.

---

## الفرق بين الحلين

| | حلول ضعيفة (لو اتوقفنا عندها) | الحل القوي (الفعلي) |
|---|---|---|
| **بتعالج** | أعراض جانبية (Timeout بطيء، SW ناقص، onLine مش دقيق) | السبب الجذري نفسه (تفسير غلط لنتيجة `getSession()`) |
| **بتتأكد بيها** | افتراضات نظرية ("لازم يكون كذا") | فحص فعلي لكود supabase-js المصدري + محاكاة حقيقية في المتصفح |
| **بتغطي** | حالة واحدة بس (مثلاً الويب فقط، أو أول فتح فقط) | كل نقطة استخدام (`restoreSession` و`SB.req` كمان) |
| **بتفرّق** | لأ — أي فشل = تسجيل خروج | بدقة بين فشل شبكة ورفض حقيقي |

النتيجة: الإصلاح النهائي اتنشر فعليًا (v1.1.1) واتأكد حي في المتصفح
بمحاكاة السيناريو الحقيقي (جلسة صالحة + فشل شبكة عند التجديد) — رجع
`authState: offline_session` والتطبيق فتح فعليًا بالبيانات المحلية،
بدل ما يرجع لصفحة الدخول زي قبل كده.
