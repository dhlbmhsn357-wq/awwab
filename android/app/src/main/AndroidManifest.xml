<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Links: awwab://today ، awwab://calendar ...إلخ —
                 بيتلقّطها App.addListener('appUrlOpen') في index.html.
                 مستخدمة داخليًا لفتح التطبيق على تبويب معيّن عند الضغط
                 على إشعار محلي، ومتاحة لأي استخدام مستقبلي تاني. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="awwab" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- إشعارات محلية (تذكير يومي) — أندرويد 13+ محتاج إذن وقت التشغيل
         صراحة، بيتطلب فعليًا عبر LocalNotifications.requestPermissions()
         في index.html، مش تلقائي بمجرد التصريح هنا -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- عشان التذكير يوصل في الوقت المحدد بالظبط (مش بعد تأخير النظام
         الموفّر للبطارية) — بيتطلب موافقة صريحة من المستخدم على
         أندرويد 12+ لو مفعّلة، وبنعامله كـfallback غير حرج لو اتمنعت -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
</manifest>
