# أوّاب — تجهيز إصدار Android موقّع (Release)

## ⚠️ إجراء مطلوب منك دلوقتي

مفتاح توقيع الإصدار (Keystore) اتنشأ فعلاً — بإذنك الصريح — وموجود على
جهازك في:

```
android/awwab-release.keystore
android/keystore.properties
```

**اعمل نسخة احتياطية من الملفين دول دلوقتي** (مدير باسورد يدعم
مرفقات، تخزين سحابي مشفّر، USB في مكان آمن... أي حاجة غير جهازك بس)
— لو الجهاز ده اتعطّل أو الملفين اتفقدوا، مفيش نسخة تانية منهم عند
حد، لا أنا ولا GitHub (Secrets مكتوبة-فقط، محدش يقدر يقراها تاني بعد
الرفع). فقدهم يعني مقدرش تنشر تحديث فوق النسخة الحالية للأبد — هيبقى
لازم تطبيق جديد بـpackage ID مختلف من الصفر.

الملفين دول **متسجّلينش في Git خالص** (`.gitignore`) — ده مقصود.

## الحالة الحالية: التوقيع شغّال فعليًا

- المفتاح نفسه (base64) وكلمات السر مرفوعين كـGitHub Secrets:
  `ANDROID_KEYSTORE_BASE64`، `ANDROID_KEYSTORE_PASSWORD`،
  `ANDROID_KEY_ALIAS`، `ANDROID_KEY_PASSWORD`.
- `.github/workflows/android-release.yml` بيفك تشفيرهم وقت البناء
  بس، ويمسحهم من قرص الـrunner فور ما البناء يخلص (`if: always()`).
- كل إصدار بيتبني عبر `./gradlew assembleRelease` (مش `assembleDebug`
  زي الأول) — يعني **موقّع فعليًا بنفس المفتاح في كل مرة**، فالتحديثات
  بقت تتثبت فوق بعضها من غير ما تحتاج حذف التطبيق القديم.
- `versionCode` بياخد رقم تشغيلة الـworkflow نفسها (`github.run_number`)
  تلقائيًا — دايمًا متزايد، مفيش تعارض بين إصدارين.

## لإصدار نسخة جديدة

من تبويب **Actions** في الريبو، شغّل workflow **"Android Release"**
يدويًا (`workflow_dispatch`)، واملأ:
- **version**: رقم الإصدار (مثال `1.1.0`) — سيمانتيك فيرجن عادي.
- **releaseNotes**: وصف مختصر بيظهر للمستخدم جوه Bottom Sheet التحديث.
- **mandatory**: فعّلها بس لو فيه مشكلة أمان خطيرة (نادر جدًا).

الـworkflow بيعمل كل حاجة تلقائيًا: بناء موقّع → نشر GitHub Release
عام → تحديث `latest-release.json` (فيه `versionCode` الجديد، رابط
الـAPK، حجمه، وSHA-256 checksum) → دفعه لـmain. صفحة `/download`
وفاحص التحديث جوه التطبيق بيعكسوا الإصدار الجديد فورًا.

## دورة تحديث المستخدم (زي ما هي فعليًا)

1. المستخدم فاتح التطبيق، بعد ما الجلسة تستقر، فحص خلفي (مايأخّرش
   فتح التطبيق) يقارن `versionCode` المثبّت بأحدث نسخة منشورة.
2. لو فيه أحدث، Bottom Sheet تظهر: "يتوفر تحديث جديد لأوّاب" مع زرار
   "تحديث الآن" و"لاحقًا" (لو مش Mandatory).
3. "تحديث الآن" بيحمّل الـAPK جوه التطبيق نفسه (DownloadManager،
   شريط تقدّم حي)، وبمجرد ما يخلص بيفتح **مثبّت أندرويد الرسمي**
   تلقائيًا — المستخدم بيضغط "تثبيت" بنفسه (مفيش Silent Install،
   ده غير مسموح لأي تطبيق sideloaded على أندرويد أصلاً).
4. أول مرة، أندرويد ممكن يطلب إذن "تثبيت من مصادر غير معروفة" —
   التطبيق بيوجّه المستخدم لشاشة الإذن مباشرة لو محتاجه.
5. بعد التثبيت، الجلسة والبيانات المحلية (IndexedDB) والإعدادات
   وطابور المزامنة كلها بتفضل زي ما هي — التحديث بيحصل فوق نفس
   التطبيق، مش تثبيت جديد من الصفر.

## R8 / ProGuard (تصغير وتعتيم الكود)

`minifyEnabled` متسيّب `false` عمدًا في `android/app/build.gradle`.
القواعد اللازمة لـCapacitor والـplugins المحلية
(`android/app/proguard-rules.pro`) جاهزة ومظبوطة، بس تفعيل R8 محتاج
اختبار حقيقي على جهاز بعدها — لو حاجة اتحذفت غلط بالـreflection
(بعيد لكن ممكن)، التطبيق ممكن يوقّع بصمت من غير أي رسالة خطأ واضحة
وقت البناء. لما يكون عندك جهاز تختبر عليه:

```gradle
buildTypes {
    release {
        minifyEnabled true   // بدّلها من false
        ...
    }
}
```

ابني، ثبّت، وجرّب كل شاشات التطبيق (تسجيل دخول، تسجيل عبادة، أوفلاين،
الودجت، التحديث) قبل ما تعتمد على النسخة دي.

## لو المفتاح اتفقد أو محتاج تدوير (Rotation)

ما ينفعش — نفس السبب بالظبط اللي فوق. لو حصل، الحل الوحيد تطبيق جديد
بـ`applicationId` مختلف، وكل المستخدمين الحاليين هيحتاجوا يثبتوه من
جديد ويسجّلوا دخول تاني (بياناتهم في Supabase متأثرتش، بس التطبيق
نفسه هيبقى تنصيب مختلف من وجهة نظر أندرويد).

## رفع Google Play (أول مرة)

خارج نطاق هذا المستند — يحتاج حساب Google Play Console (رسوم لمرة
واحدة)، صفحة تطبيق (وصف، صور، سياسة خصوصية)، ومراجعة Google. البنية
الحالية جاهزة لكده من غير أي إعادة بناء — نفس الـkeystore ونفس
هيكلة الإصدار. لو قررت تنشر فعليًا، قولّي وهساعدك تجهّز كل حاجة
تقنية لازمة (Privacy Policy، Data Safety form، استبدال الـUpdater
الحالي بـGoogle Play In-App Updates، AAB نهائي...).
