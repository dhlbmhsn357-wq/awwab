# أوّاب — نسخة Android (Capacitor)

طبقة Android حقيقية فوق نفس تطبيق الويب/PWA الموجود — نفس الحساب،
نفس قاعدة البيانات (Supabase)، نفس منطق العمل بالكامل. الويب/PWA
مايتأثرش خالص بوجود النسخة دي.

## البنية

```
index.html, manifest.json, sw.js, icon-*.png   ← المصدر الحقيقي الوحيد للكود (زي ما هو دايمًا)
capacitor.config.ts                             ← إعداد Capacitor (appId: com.awwab.app)
scripts/prepare-www.mjs                         ← بينسخ (مش يعدّل) الملفات دي لـwww/ قبل أي cap sync
www/                                             ← مولّد تلقائيًا، متسجلش في Git
android/                                         ← مشروع Android الحقيقي (Gradle)
.github/workflows/android-build.yml             ← بناء APK سحابيًا (Actions)
```

## ليه مفيش Build محلي هنا

الجهاز اللي بيتطوّر عليه المشروع حاليًا مفيهوش Android SDK/Java. كل
بناء الـAPK بيحصل عبر GitHub Actions (workflow `android-build.yml`)،
والنتيجة بتترفع كـArtifact قابل للتحميل من تبويب **Actions** في
الريبو (اسم الـArtifact: `awwab-debug-apk`).

لو عندك Android Studio مثبّت محليًا بدل كده:
```bash
npm install
npm run cap:sync      # بينسخ الويب لـwww/ ويزامن مشروع android/
npm run cap:open:android   # يفتح المشروع في Android Studio
```

## Package ID و App Name

- **Package ID**: `com.awwab.app` — ثابت، منتغيرش أبدًا بعد كده
  (مرتبط بأي تحديث مستقبلي/نشر على المتجر).
- **App Name**: أوّاب (بالعربي، ظاهر صح في Launcher — مُتحقق منه في
  `android/app/src/main/res/values/strings.xml`).

## الحالة الحالية (Phase 1 — مكتملة)

- ✅ مشروع Android مولّد (`npx cap add android`)، appId
  `com.awwab.app`، اسم التطبيق "أوّاب" ظاهر صح في الموارد.
- ✅ خط بناء APK سحابي شغّال وناجح (GitHub Actions، JDK 21 + Node 22
  — Capacitor 8 محتاجهم بالظبط).
- ✅ اتأكد إن ملفات `www/` (المنسوخة لـAndroid) مطابقة بايت-لبايت
  لملفات الويب الحقيقية.
- ✅ اتأكد إن نشر الويب/PWA على Vercel مش متأثر خالص (قبل وبعد إضافة
  ملفات Android، فحصت الموقع الحي مباشرة).
- ⏳ Auth/Offline/Widget/Notifications — مراحل لاحقة، راجع خطة
  التنفيذ في المحادثة.

## الحالة الحالية (Phase 2 — مكتملة)

- ✅ أيقونة حقيقية بدل الافتراضية: `scripts/generate-android-icons.py`
  بيولّدها من `icon-512.png` نفسه (نفس أيقونة الويب/PWA، بدون تصميم
  جديد) — Legacy (بالشعار الكامل)، Adaptive foreground (الشعار بس،
  محطوط في المنطقة الآمنة عشان مايتقصش)، وRound، لكل الكثافات
  (mdpi→xxxhdpi).
- ✅ لون خلفية الأيقونة التكيّفية (`ic_launcher_background.xml`)
  مأخوذ من نفس لون خلفية الشعار الحقيقي (`#F2F0F1`).
- ✅ Splash Screen ببراند أوّاب (نفس لون الخلفية + الشعار الكامل في
  النص) بدل شاشة Capacitor الفاضية الافتراضية، لكل الكثافات
  واتجاهي الشاشة (port/land).
- ✅ ألوان شريط الحالة/التنقّل (`status/navigation bar`) بلون هوية
  أوّاب الأساسي (`#015C51`) بدل الأزرق الافتراضي، مع أيقونات فاتحة
  (`windowLightStatusBar=false`) لتباين واضح فوق اللون الغامق.
- ✅ `colors.xml` (كان ناقص من مشروع Capacitor المولّد تلقائيًا،
  رغم إن `styles.xml` بيرجع له) — اتضاف بألوان الهوية الحقيقية.

لإعادة توليد الأيقونات/الـSplash بعد أي تغيير مستقبلي في
`icon-512.png`:
```bash
python scripts/generate-android-icons.py
```

## الحالة الحالية (Phase 3 — مكتملة)

- ✅ **اكتشاف مهم أثناء الفحص**: Dexie وsupabase-js كانا محمّلين من
  CDN خارجي (`cdn.jsdelivr.net`) مباشرة في `index.html`، والـService
  Worker متعمّدًا مايكاشش أي أصل خارجي (`sw.js` كان بيسيب أي طلب مش
  same-origin للمتصفح يتصرف فيه لوحده). ده معناه لو الجهاز فتح
  التطبيق أوفلاين (خصوصًا أول تشغيل بعد التثبيت على أندرويد، أو لو
  كاش الـWebView اتمسح) والمكتبتين دول متحمّلوش، **التطبيق كله كان
  ممكن يفشل يفتح من الأساس** — رغم إنه مصمم يشتغل أوفلاين بالكامل.
- ✅ **الحل**: المكتبتين اتسحبوا محليًا (`vendor/dexie.min.js`،
  `vendor/supabase.js`) بنفس المحتوى بالظبط (اتوثّق بـSHA-256 وقت
  النسخ: Dexie 4.0.8، supabase-js 2.112.3 — نفس النسخة اللي كانت شغالة
  فعليًا من `@2` قبل كده)، وبقوا same-origin. اتضافوا لقائمة الملفات
  اللي الـSW بيكاشها من أول تشغيل (`APP_SHELL` في `sw.js`)، ولقائمة
  النسخ لـwww/ (`scripts/prepare-www.mjs`) — نفس الملف شغال على
  الويب/PWA والأندرويد بدون أي فرق.
- ✅ CSP على Vercel (`vercel.json`) اتشدّدت تبعًا لكده — إزالة
  `https://cdn.jsdelivr.net` من `script-src` بعد ما بقت مش مستخدمة
  خالص (كل الـscripts دلوقتي `'self'`).
- ✅ Auth/Session: `supabase-js` الرسمي (`persistSession: true,
  autoRefreshToken: true`) — بيخزن الجلسة في `localStorage`، ومتاح
  ومستمر طبيعي جوه WebView أندرويد الحديث بدون أي تعديل كود. محتاج
  تأكيد حقيقي منك على جهازك (سجّل دخول، اقفل التطبيق تمامًا من
  الخلفية، افتحه تاني، وشوف لو لسه مسجّل دخول).
- ✅ **Routing (زرار الرجوع بالأندرويد)**: التطبيق SPA بدون
  URL routing حقيقي، فزرار الرجوع الافتراضي في أندرويد كان هيقفل
  التطبيق فورًا من أول ضغطة حتى لو فيه نافذة/جولة تعريفية مفتوحة —
  اتضاف معالج مخصّص (`@capacitor/app` → `backButton`):
  1. لو الجولة التعريفية شغالة → تتقفل (زي زرار Escape بالظبط).
  2. لو فيه أي نافذة منبثقة مفتوحة (إضافة عبادة/إجراء/اختيار تاريخ/
     ترحيب/استئناف) → تتقفل هي بس.
  3. لو المستخدم في تبويب غير "اليوم" → يرجع لتبويب "اليوم".
  4. لو أصلًا في "اليوم" وملهوش حاجة مفتوحة → السلوك الافتراضي
     (تصغير/إغلاق التطبيق زي أي تطبيق أندرويد عادي).

## الحالة الحالية (Phase 4 — مكتملة)

- ✅ **الشبكة**: `@capacitor/network` بيحل محل الاعتماد على
  `navigator.onLine`/أحداث `online`/`offline` وقت التشغيل على
  أندرويد بس (الويب/PWA لسه شغال بنفس الطريقة القديمة تمامًا) —
  Network plugin بيقرا حالة الاتصال من نظام أندرويد نفسه
  (`ConnectivityManager`)، أدق من حالة WebView الداخلية.
- ✅ **دورة الحياة (Lifecycle)**: `@capacitor/app` → `appStateChange`
  بيلتقط رجوع التطبيق من الخلفية بدقة (`onResume` الحقيقي)، ويشغّل
  فحص شبكة + مزامنة فورية — بديل أدق من `document.visibilitychange`
  اللي مش مضمون دايمًا جوه WebView حسب إصدار أندرويد/الشركة المصنّعة.
  الـ`visibilitychange` وmؤقّت الـ90 ثانية القديمين لسه موجودين
  كـfallback إضافي، ما اتشالوش.
- ✅ الـPlugins اتسجّلوا فعليًا في مشروع Android (`npx cap sync
  android`) وتأكد إن `capacitor.settings.gradle` بيشوفهم.

## الحالة الحالية (Phase 5 — مكتملة)

- ✅ **تذكير يومي محلي** (`@capacitor/local-notifications`): مجموعة
  إعدادات جديدة في الإعدادات ← "التذكير اليومي" (ظاهرة على أندرويد
  بس)، بتفعّل/تلغي تذكير في وقت تختاره، مع طلب إذن الإشعارات
  بشكل صحيح (أندرويد 13+). التفضيل ده جهاز-محلي بحت (`localStorage`)
  ومش بيتزامن مع Supabase — كل جهاز له تذكيره الخاص.
- ✅ أيقونة شريط الحالة (`ic_stat_notify`) اتولّدت من نفس شعار
  أوّاب (Silhouette أبيض — أندرويد بيتجاهل الألوان في أيقونات
  الإشعارات أصلًا) بدل الأيقونة الرمادية الافتراضية، بلون
  `#017C6E` (هوية أوّاب) بدل الأخضر الافتراضي.
- ✅ **Deep Links** (`awwab://today`، `awwab://calendar`...إلخ):
  intent-filter جديد في `AndroidManifest.xml` + معالج
  `App.addListener('appUrlOpen')`. الاستخدام الأساسي دلوقتي: الضغط
  على إشعار التذكير بيودّي لتبويب "اليوم" مباشرة (`localNotification
  ActionPerformed` بينده على نفس دالة التوجيه).
- ✅ الصلاحيات المطلوبة (`POST_NOTIFICATIONS`) اتضافت لـ
  `AndroidManifest.xml` بشكل صريح (فوق اللي الـplugin بيضيفه تلقائيًا
  عبر دمج الـmanifest)، واتوثّق إن `SCHEDULE_EXACT_ALARM` تعامل
  كـ"أفضل محاولة" مش حرج — تذكير تقريبي في الوقت المحدد كافي تمامًا
  لطبيعة الميزة، مش منبّه دقيق.

**محتاج تأكيد حقيقي منك على جهازك (Phase 5):**
1. من الإعدادات، فعّل "تذكير بتسجيل عباداتي" ووافق على إذن
   الإشعارات لو طلبه أندرويد.
2. اختار وقت قريب (بعد دقيقة-دقيقتين) وشوف هل الإشعار بيوصل في وقته
   تقريبًا، وهل شكل الأيقونة ولونها ظاهرين صح في شريط الحالة.
3. اضغط على الإشعار — لازم يفتح التطبيق على تبويب "اليوم" مباشرة.
4. اقفل التطبيق تمامًا وجرّب تاني (تأكيد إن التذكير بيفضل شغال حتى
   لو التطبيق مش فاتح).

**محتاج تأكيد حقيقي منك على جهازك (Phase 3+4 مع بعض):**
1. سجّل دخول، اقفل التطبيق بالكامل من قائمة التطبيقات الأخيرة، افتحه
   تاني — لازم يفضل مسجّل دخول من غير ما يطلب تسجيل دخول تاني.
2. افتح التطبيق وشغّل وضع الطيران (Airplane mode) — التطبيق لازم
   يفضل شغال ويعرض بياناتك المحفوظة محليًا (Offline-first).
3. سجّل عبادة وأنت في وضع الطيران، اقفل الوضع، وشوف هل بتتزامن مع
   السيرفر تلقائيًا خلال ثواني من رجوع النت.
4. افتح نافذة (زي "إضافة عبادة")، اضغط زرار الرجوع الفعلي بالموبايل
   — النافذة المفروض تتقفل بس، مش التطبيق كله.
5. من تبويب غير "اليوم"، اضغط زرار الرجوع — المفروض يرجّعك لتبويب
   "اليوم" بدل ما يقفل التطبيق.

## الحالة الحالية (Phase 6+7 — مكتملة): ودجت الشاشة الرئيسية

**القرار المعماري الأهم** (بند 91 من المتطلبات الأصلية): الودجت
**أبدًا** ماتكلّمش Supabase مباشرة، مش بالـanon key ولا أي مفتاح
تاني. بتقرا بس من `SharedPreferences` محلية صرفة (`awwab_widget_
prefs`) — أي بيانات فيها اتحسبت مسبقًا جوه الـWebView نفسه (نفس منطق
`isApplicable()` المستخدم في شاشة "اليوم" بالظبط، من IndexedDB
المحلية، بدون أي طلب شبكة) وبقت "لقطة" جاهزة.

**تدفّق البيانات:**
```
index.html (IndexedDB محلي)
  → collectAndSyncWidgetSnapshot()
  → WidgetBridge.updateSnapshot(...)  [Capacitor plugin مخصّص]
  → WidgetBridgePlugin.java يكتب SharedPreferences + يطلب تحديث فوري
  → AwwabWidgetProvider.java يقرا SharedPreferences ويرسم RemoteViews
```

بيتنادى تلقائيًا في 3 لحظات: بعد أي تسجيل/تعديل عبادة (`upsertLog`)،
بعد تحميل العبادات عند فتح التطبيق (`loadWorships`)، وعند رجوع
التطبيق من الخلفية (`appStateChange`). فشل صامت مقصود لو أي خطوة
فشلت — الودجت ميزة إضافية، مايأثرش على التطبيق نفسه أبدًا.

**تصميم الودجت نفسها**: للقراءة بس، عرض "٥ من ٨ اليوم" + اسم أول
عبادة لسه ماتسجّلتش. لمسة واحدة بس متاحة (الودجت كله) وبتفتح
التطبيق على تبويب "اليوم" عبر Deep Link (`awwab://today`) — **مفيش
أي Quick Action بيعدّل بيانات من جوه الودجت نفسها** (بند 37: الأمان
قبل السرعة — أي تسجيل عبادة لازم يمر بشاشة التطبيق).

**ملفات جديدة:**
- `WidgetBridgePlugin.java` — الجسر (Capacitor plugin محلي، مش من npm).
- `AwwabWidgetProvider.java` — الودجت نفسها (`AppWidgetProvider`).
- `res/layout/widget_awwab.xml` + `res/xml/widget_awwab_info.xml` —
  الشكل والإعدادات (حجم Medium افتراضي، قابل للتحجيم).
- ألوان فاتح/داكن منفصلة (`values/colors.xml` + `values-night/colors.xml`)
  عشان الودجت يتناسق مع خلفية الشاشة الرئيسية في الوضعين.

**ملاحظة تقنية**: اتكتبت بـJava (مش Kotlin) عمدًا — المشروع أصلًا
Java (`MainActivity.java` من توليد Capacitor)، وإضافة Kotlin toolchain
جديدة كانت هتزود احتمالية فشل بناء ما ينفعش يتشخّص محليًا (مفيش
Android SDK هنا، كل تحقق من نجاح الكومبايل بيحصل عبر GitHub Actions
بس) من غير أي فايدة حقيقية.

**محتاج تأكيد حقيقي منك على جهازك (Phase 6+7):**
1. من شاشة الودجتس بالموبايل (اضغط مطوّل على الشاشة الرئيسية ←
   ودجتس)، دوّر على "أوّاب" واسحبه للشاشة الرئيسية.
2. لازم يظهر فورًا بحالة اليوم الحالية (مش فاضي/"افتح التطبيق").
3. سجّل عبادة من داخل التطبيق، ارجع للشاشة الرئيسية — الرقم في
   الودجت لازم يتحدّث خلال ثواني من غير ما تعمل حاجة تانية.
4. اضغط على الودجت — لازم يفتح التطبيق على تبويب "اليوم" مباشرة.
5. جرّب في الوضع الداكن والفاتح — الألوان لازم تكون متناسقة وواضحة
   في الحالتين.

## الحالة الحالية (Phase 8 — مكتملة): إعدادات الودجت + الخصوصية

- ✅ قسم جديد في الإعدادات ← "ودجت الشاشة الرئيسية" (أندرويد بس)،
  فيه "وضع الخصوصية": لما يتفعّل، الودجت بيعرض اسم التطبيق بس
  ("افتح التطبيق للمتابعة") من غير أي رقم أو اسم عبادة — مفيد لو
  حد تاني ممكن يشوف شاشتك الرئيسية.
- ✅ التفضيل ده جهاز-محلي (`localStorage`)، وبيتبعت مع كل لقطة
  للودجت (`privacyMode`)، والقرار الفعلي بيتاخد في الكود النيتف
  (`AwwabWidgetProvider.java`) — مش مجرد إخفاء بصري في الواجهة.

## الحالة الحالية (Phase 9 — مكتملة): خطوة الودجت في الجولة التعريفية

- ✅ خطوة جديدة في `TOUR_STEPS` بعد خطوة تثبيت PWA مباشرة، بتشاور
  على قسم "ودجت الشاشة الرئيسية" في الإعدادات وتشرحه باختصار.
- ✅ `skipIf:()=>!IS_NATIVE` — بتتخطى تلقائيًا وبالكامل لمستخدمي
  الويب/PWA (نفس نمط خطوة التثبيت بالظبط)، فمفيش أي تأثير على تجربة
  الجولة على الويب.

## الحالة الحالية (Phase 10 — مكتملة): تجهيز الإصدار النهائي

- ✅ **الإصدار (Versioning)**: `versionName` اتظبط لـ`1.0.0`
  (Semantic Versioning)، `versionCode` لسه `1` (صح — أول إصدار
  فعلي لسه ماتنشرش).
- ✅ **توقيع الإصدار (Signing)**: بنية جاهزة بالكامل في
  `android/app/build.gradle` بتقرا `android/keystore.properties`
  لو موجود وتوقّع بيه، وترجع لبناء غير موقّع (للتطوير) لو مش موجود
  — **الملف نفسه ماتعملش عمدًا**، لأن مفتاح التوقيع الحقيقي هوية
  دائمة للتطبيق على Google Play ولازم يبقى تحت سيطرتك إنت بس، مش
  حاجة أي أداة تلقائية تنشئها أو تحتفظ بيها. الخطوات كاملة (إنشاء
  الـkeystore، تعبئة الإعدادات، بناء إصدار موقّع) موثّقة في
  [RELEASE.md](RELEASE.md).
- ✅ **R8/ProGuard**: قواعد Capacitor الصحيحة جاهزة في
  `android/app/proguard-rules.pro` (حماية الـplugins من الحذف/إعادة
  التسمية بالـreflection)، بس `minifyEnabled` متسيّب `false` عمدًا —
  تفعيله من غير جهاز حقيقي يتاختبر عليه بعده خطر حقيقي (تعتيم غلط
  ممكن يوقّف التطبيق بصمت من غير رسالة خطأ وقت البناء). تفصيل القرار
  والخطوات لما تكون جاهز في RELEASE.md.
- ✅ خط بناء الـdebug APK الحالي (GitHub Actions) فضل زي ما هو
  بالظبط — مفيش أي تغيير كسر أو أثّر على البناء الحالي.

## تجربة تحميل التطبيق من داخل الموقع (بدون GitHub خالص)

مستخدمي الويب مايحتاجوش يعرفوا إن GitHub موجود في القصة أصلًا:

- **`/download`**: صفحة تحميل مستقلة (`download.html`) بهوية أوّاب
  الكاملة، بتكتشف نوع الجهاز (أندرويد/آيفون/ديسكتوب) وتعرض المحتوى
  المناسب لكل حالة. زر التحميل بيوديك مباشرة لملف الـAPK (استضافة
  الملف الفعلية GitHub Release، لكن المستخدم مايشوفش أي صفحة GitHub —
  تحميل مباشر بس).
- **`latest-release.json`**: ملف صغير في جذر الموقع بيحمل رقم آخر
  إصدار ورابطه وحجمه — بيتحدّث تلقائيًا (مش يدويًا) مع كل إصدار جديد
  عبر `.github/workflows/android-release.yml`.
- **بانر في الموقع**: يظهر لمستخدمي أندرويد اللي بيستخدموا نسخة
  الويب بس (مش النيتف)، مرة واحدة كل جلسة، ومايتصادمش مع بانر تثبيت
  الـPWA.
- **الإعدادات ← "تطبيق أندرويد"**: زر لصفحة التحميل لمستخدمي الويب،
  أو رقم الإصدار الحالي (`App.getInfo()`) لمستخدمي التطبيق النيتف.
- **خطوة في الجولة التعريفية**: بعد خطوة الودجت مباشرة، لمستخدمي
  أندرويد-ويب بس (`skipIf: IS_NATIVE || !isAndroidUA()`).

**لإصدار نسخة جديدة**: من تبويب Actions في الريبو، شغّل workflow
"Android Release" يدويًا (`workflow_dispatch`) — بيبني الـAPK، ينشئه
كـGitHub Release عام، ويحدّث `latest-release.json` تلقائيًا. صفحة
`/download` بتعكس الإصدار الجديد فورًا من غير أي تعديل كود يدوي.

## Repair + Polish + Update System Pass (بعد الإصدار الأول)

طلب شامل من المستخدم لإصلاح 4 مشاكل رئيسية اتكشفت بعد الاستخدام
الفعلي — كل حاجة اتنفذت:

**1) الجولة التعريفية**: خطوة تثبيت الـPWA كانت بتظهر (بمحتوى غير
مرتبط) لمستخدمي أندرويد اللي أصلاً عندهم بديل حقيقي أفضل — اتصلحت
(`skipIf` بقت تتحقق من `IS_NATIVE`/`isAndroidUA()`، مش بس
`isStandaloneMode()`)، ونفس الإصلاح اتطبّق على قسم الإعدادات وبانر
التثبيت العائم. خطوة جديدة اتضافت تشاور فعليًا على زرار "أضفها الآن"
الحقيقي جوه إعدادات الودجت.

**2) إضافة الودجت بزر مباشر**: `AppWidgetManager.
requestPinAppWidget()` الرسمية (plugin جديد `WidgetPinPlugin.java`)
— ضغطة واحدة على "أضفها الآن" في الإعدادات (أو من الجولة) بتفتح
نافذة تأكيد أندرويد الرسمية مباشرة، من غير ما المستخدم يدوّر يدويًا.
لو الجهاز/الـLauncher مايدعمش الـAPI (نادر)، Bottom Sheet بتعليمات
مختصرة كـfallback بس. نجاح الإضافة بيتأكد بمقارنة عدد الودجتس قبل/
بعد عند رجوع التطبيق، وبيظهر Toast تأكيد.

**3) إعادة تصميم الودجت**: كثافة معلومات أعلى بمساحة فاضية أقل —
رأس مضغوط (أيقونة 13dp + نسبة اليوم)، `٦ / ١٠ اليوم` بخط أوضح،
Progress Bar رفيع (4dp) بدل أي Ring كبير، سطر "التالي" مع تلميح
"فتح ›". حالة اكتمال اليوم منفصلة ("بارك الله فيك ✓"). وضع الخصوصية
بقى أدق: اسم العبادة بس بيتبدّل بـ"العبادة التالية" (مش كل حاجة
بتتخفي زي الأول)، والنسبة تفضل ظاهرة. الحجم الافتراضي اتصغّر
(180×90dp بدل 250×110dp)، لسه قابل للتحجيم بحرية.

**4) نظام تحديث داخل التطبيق**: أهم جزء تقني في الدفعة دي — راجع
[RELEASE.md](RELEASE.md) للتفاصيل الكاملة. ملخص:
- **مفتاح توقيع دائم** اتنشأ (بإذن صريح من المستخدم بعد ما حاولت
  إنشاءه محليًا واتمنعت من نظام الحماية تلقائيًا لحساسية القرار) —
  محفوظ كـGitHub Secrets، وكل إصدار دلوقتي موقّع بيه فعليًا
  (`assembleRelease` بدل `assembleDebug`)، يعني التحديثات بقت تتثبت
  فوق بعضها من غير حذف.
- `versionCode` بياخد رقم تشغيلة الـworkflow تلقائيًا (دايمًا
  متزايد، بلا تعارض).
- فاحص تحديث خلفي (`checkForAppUpdate`) بيتنادى بعد ما الجلسة
  تستقر + عند كل رجوع من الخلفية (بحد أقصى مرة كل نص ساعة تلقائيًا)
  — بيقارن مع `latest-release.json` على الموقع الحي (نفس الملف اللي
  صفحة `/download` بتستخدمه، ملف واحد بدل اتنين عشان مايحصلش تعارض).
  زرار يدوي "التحقق من وجود تحديث" في الإعدادات كمان.
- Bottom Sheet تحديث (`plugin` جديد `UpdaterPlugin.java`): تحميل
  الـAPK جوه التطبيق نفسه (`DownloadManager` + شريط تقدّم حي) ثم فتح
  مثبّت أندرويد الرسمي تلقائيًا — **مفيش Silent Install** (غير مسموح
  أصلاً لتطبيق sideloaded)، المستخدم بيضغط "تثبيت" بنفسه مرة واحدة.
  توجيه تلقائي لشاشة إذن "التثبيت من مصادر غير معروفة" لو محتاج.
- دعم `mandatory=true` (تحديث إجباري، للأمان بس) و"لاحقًا" مع تذكير
  بعد 24 ساعة (مش إزعاج متكرر).
- الجلسة/IndexedDB/الإعدادات/طابور المزامنة — كلها بتفضل زي ما هي
  بعد التحديث (التثبيت فوق نفس التطبيق، مش تنصيب جديد).

**محتاج تأكيد حقيقي منك على جهازك (Repair Pass):**
1. من الإعدادات، اضغط "أضفها الآن" — لازم تظهر نافذة تأكيد أندرويد
   الرسمية مباشرة (مش قائمة Widgets العادية).
2. شوف شكل الودجت الجديد — أصغر وأكتف من الأول، وشوف حالة "بارك الله
   فيك" لما تخلّص كل عبادات اليوم.
3. أعد الجولة التعريفية من الإعدادات — تأكد إن خطوات الودجت وتثبيت
   أندرويد بتشاور فعليًا على العناصر الصحيحة.
4. من الإعدادات ← تطبيق أندرويد، دوس "التحقق من وجود تحديث" — لازم
   يقولك "أنت تستخدم أحدث إصدار" (لسه نفس النسخة).
5. لما ينزل إصدار أحدث فعليًا، تأكد إن Bottom Sheet التحديث بتظهر،
   التحميل بيشتغل، والمثبّت الرسمي بيفتح تلقائيًا بعد ما يخلص —
   وإن حسابك وبياناتك وودجتك تفضل زي ما هي بعد التثبيت.

## اختبار حي

كل تحقق من سلوك حقيقي على جهاز (استمرار الجلسة، الأوفلاين، الودجت)
محتاج تثبيت الـAPK على موبايل Android حقيقي — مفيش إيموليتر أو جهاز
متاح في بيئة التطوير الحالية.
